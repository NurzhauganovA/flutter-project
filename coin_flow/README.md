# CoinFlow — Умный трекер личных финансов

CoinFlow — это современное мобильное приложение на Flutter, разработанное для тех, кто хочет держать свои финансы под контролем без лишних усилий. Проект сочетает в себе чистый UI, мощную аналитику и облачную синхронизацию данных в реальном времени.

## 📱 Ключевые возможности

Приложение закрывает все базовые потребности по ведению бюджета и предлагает ряд продвинутых функций:

* **Управление транзакциями**: Быстрое добавление доходов и расходов с указанием категории, суммы, даты и заметки.
* **Интерактивная аналитика**: Наглядные круговые диаграммы (Pie Charts) и прогресс-бары для оценки структуры трат. Фильтрация статистики по неделям, месяцам и годам.
* **Персонализация профиля**: Пользователи могут установить свое имя и загрузить уникальный аватар (поддерживается съемка с камеры и выбор из галереи).
* **Умный поиск**: Мгновенная фильтрация транзакций по названиям категорий, описанию или суммам.
* **Темная тема**: Полноценная поддержка Dark Mode, которая синхронизируется с системными настройками или переключается вручную внутри приложения.
* **Безопасная авторизация**: Вход и регистрация через Email/Пароль с использованием Firebase Auth.

## 🛠 Технологический стек

В разработке использовались актуальные подходы и библиотеки экосистемы Flutter:

* **Flutter & Dart**: Использование SDK версии 3.10+ для обеспечения высокой производительности и плавных анимаций.
* **State Management**: `flutter_riverpod` — для реактивного управления состоянием, инъекции зависимостей и чистой архитектуры.
* **Backend & Cloud**:
    * **Firebase Auth**: Управление сессиями пользователей.
    * **Cloud Firestore**: NoSQL база данных для хранения истории операций в реальном времени.
    * **Firebase Storage**: Облачное хранилище для медиафайлов (аватарок).
* **Навигация**: `go_router` — декларативный роутинг с поддержкой вложенной навигации (ShellRoute) для сохранения состояния нижней панели (Bottom Bar).
* **UI/UX**:
    * Шрифт **Inter** (через `google_fonts`) для современной типографики.
    * `fl_chart` для построения графиков.
    * Адаптивная верстка, поддерживающая iOS и Android.

## 📂 Архитектура проекта

Проект структурирован по принципу **Feature-first** (разделение по фичам), что упрощает масштабирование и командную разработку:

```text
lib/
├── core/           # Общие утилиты, темы, базовые модели и виджеты
├── features/       # Модули функциональности:
│   ├── auth/       # Логика входа и регистрации
│   ├── home/       # Главный экран и лента операций
│   ├── transactions/# Добавление и управление транзакциями
│   ├── dashboard/  # Экран аналитики
│   ├── profile/    # Управление профилем и аватаром
│   └── settings/   # Настройки приложения
├── navigation/     # Конфигурация роутера
└── main.dart       # Точка входа